@startuml
class MainApplication {
  +void main()
  -void SystemClock_Config()
  -void MX_GPIO_Init()
  -void MX_USART2_UART_Init()
  -void Test_LED_Pattern()
  -void Test_UART_Communication()
}

class GPIO_Driver {
  +void HAL_GPIO_Init()
  +void HAL_GPIO_TogglePin()
}

class UART_Driver {
  +void HAL_UART_Init()
  +void HAL_UART_Transmit()
}

class RCC_Driver {
  +void HAL_RCC_OscConfig()
  +void HAL_RCC_ClockConfig()
}

MainApplication --> GPIO_Driver : uses
MainApplication --> UART_Driver : uses
MainApplication --> RCC_Driver : uses
@enduml 